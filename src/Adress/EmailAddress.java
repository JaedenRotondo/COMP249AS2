package Adress;
// An EmailAddress specifies a way of contacting a clinic via email. Each EmailAddress has 5 String parts:
//userName, “@”, domainName, “.”, and TLD in following form "username@domainName.TLD" (where
//Top Level Domain (TLD) can be com, org, gov, etc.).
public class EmailAddress {
    String[] emailAdress;
}
